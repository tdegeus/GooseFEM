channels:
  - conda-forge
dependencies:
  # dependencies
  - xtensor
  - eigen

  # optional dependencies
  - xsimd

  # build tools
  - cmake
  - ninja
  - setuptools_scm

  # Python API
  - python
  - numpy
  - pybind11
  - pyxtensor

  # to run tests
  - catch2
  - gmatelastic
  - gmatelastoplasticqpot

  # build docs
  - doxygen
  - mathjax

